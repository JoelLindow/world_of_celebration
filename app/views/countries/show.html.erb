<h3>Holidays of <%= @country.name %></h4>
<img class="flag_image" src="<%= @country.flag_img_url %>" alt="<%= "#{@country.name}_Flag"  %>">
<div class="wikipedia_link"></div>
<div class="google_link"></div>
