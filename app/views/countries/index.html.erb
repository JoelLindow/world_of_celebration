<% @countries.each do |country| %>
<div class="<%= "country_" + country.abbreviation %>">
  <img src="<%= country.flag_img_url %>" alt="<%= "#{country.name} flag" %>">
  <h5 class="name"><%= country.name %></h5>
  <%= link_to country.name, country_path(country.id) %>
</div>
<% end %>
